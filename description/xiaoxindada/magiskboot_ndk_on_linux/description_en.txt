This project provides build configurations for compiling Magisk's magiskboot tool using the Android NDK on Linux. It patches the magiskboot source to build standalone with NDK toolchains, producing a native binary for Android boot image unpacking, repacking, and ramdisk manipulation. It is aimed at Android developers and ROM builders who need to compile magiskboot with the NDK for custom recovery and root workflows.

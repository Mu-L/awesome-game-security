This project is an Android game mod menu template using a semi-JNI approach that combines Java overlay UI with native C++ game hooking. It provides a floating menu rendered through Android's WindowManager with toggles and sliders, while the native library handles memory patching, function hooking, and game value modification through JNI callbacks. It is aimed at Android game modders building cheat menus that combine Java UI with native code manipulation.
